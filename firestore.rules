rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // LEGACY AUTH MODE: Application uses custom auth without Firebase Auth tokens.
    // Must allow public read/write for the app to function (until refactored).
    // TRANSITION RULES: Locking down business data, keeping users open for migration.
    
    // 1. Users Collection (Open Read for Legacy Login check, Write requires Auth)
    match /users/{email} {
      allow read: if true; // Temporary: Needed for unauthenticated users to verify legacy password
      allow write: if request.auth != null; // Registration happens AFTER Auth creation
    }

    // 2. Business Data (LOCKED: Must be Authenticated)
    match /parts/{docId} { allow read, write: if request.auth != null; }
    match /shipments/{docId} { allow read, write: if request.auth != null; }
    match /vessel_tracking/{docId} { allow read, write: if request.auth != null; }
    match /equipment_tracking/{docId} { allow read, write: if request.auth != null; }
    match /customs_clearance/{docId} { allow read, write: if request.auth != null; }
    match /pre_alerts/{docId} { allow read, write: if request.auth != null; }
    match /commercial_invoices/{docId} { allow read, write: if request.auth != null; }
    match /suppliers/{docId} { allow read, write: if request.auth != null; }
    match /logistics/{docId} { allow read, write: if request.auth != null; }
    
    // 3. System Data
    match /logs/{docId} { allow read, write: if request.auth != null; }
    match /daily_changes/{docId} { allow read, write: if request.auth != null; }
    match /master_data_reports/{docId} { allow read, write: if request.auth != null; }
    match /system_metadata/{docId} { allow read: if true; allow write: if request.auth != null; } // Metadata public read ok? Parts version needs to be read by everyone? Maybe.

    // 4. Default Block (Catch-all)
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
